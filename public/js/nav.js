// add functionality to the Community button in the navbar
$('#userDshBtn').on('click', () => {
    location.replace('/user_list');
});

// add functionality to the Profile button in the navbar
$('#profileBtn').on('click', () => {
    location.replace('/user');
});
